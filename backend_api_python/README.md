# ZhiyiQuant Python Engines

This directory contains the local Python engines used by the ZhiyiQuant desktop app.

Active responsibilities:

- indicator execution
- backtest calculation
- AI analysis orchestration
- strategy runtime
- market and trading adapters
- local SQLite data access

The runtime target is the desktop app only. Configuration is loaded from local desktop settings plus optional environment overrides for development.
