# ZhiYiQuant Docker Compose
# Quick start: docker-compose up -d

version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: zhiyiquant-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-zhiyiquant}
      POSTGRES_USER: ${POSTGRES_USER:-zhiyiquant}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-zhiyiquant123}
      TZ: Asia/Shanghai
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend_api_python/migrations/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./backend_api_python/migrations:/migrations
    ports:
      - "127.0.0.1:5432:5432"
    networks:
      - zhiyiquant-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zhiyiquant -d zhiyiquant"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API Service
  backend:
    build:
      context: ./backend_api_python
      dockerfile: Dockerfile
    container_name: zhiyiquant-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      # Persistent logs
      - ./backend_api_python/logs:/app/logs
      - ./backend_api_python/data:/app/data
      # Configuration file (optional, for development)
      - ./backend_api_python/.env:/app/.env
    environment:
      - PYTHON_API_HOST=0.0.0.0
      - PYTHON_API_PORT=5000
      - TZ=Asia/Shanghai
      # Database connection
      - DATABASE_URL=postgresql://${POSTGRES_USER:-zhiyiquant}:${POSTGRES_PASSWORD:-zhiyiquant123}@postgres:5432/${POSTGRES_DB:-zhiyiquant}
      - DB_TYPE=postgresql
    networks:
      - zhiyiquant-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Frontend Web Service
  frontend:
    build:
      context: ./quantdinger_vue
      dockerfile: Dockerfile
      # Force pull from official registry to avoid mirror issues
      pull: true
    container_name: zhiyiquant-frontend
    restart: unless-stopped
    ports:
      - "8888:80"
    depends_on:
      - backend
    networks:
      - zhiyiquant-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data:
    driver: local

networks:
  zhiyiquant-network:
    driver: bridge
